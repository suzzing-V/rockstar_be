apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "common-module.fullname" . }}-test-connection"
  labels:
    {{- include "common-module.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "common-module.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
