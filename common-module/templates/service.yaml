apiVersion: v1
kind: Service
metadata:
  name: {{ include "common-module.fullname" . }}
  labels:
    {{- include "common-module.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "common-module.selectorLabels" . | nindent 4 }}
